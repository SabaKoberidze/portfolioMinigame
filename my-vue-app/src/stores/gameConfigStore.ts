import { defineStore } from 'pinia';

export const gameConfig = defineStore({
  id: 'gameConfig',
  state: () => ({
    width: 1920,
    height: 1080
  }),
  getters: {
  
  },
  actions: {
    
  },
});